# 中国历史星云宇宙：3D 星云技术方案

**V1.0 · 2026年9月24日 · 待实现与实机验证**

目标：将人物、事件与关系放入可旋转、缩放、平移的三维星云空间，形成有空间纵深、柔和星云、清晰节点和可阅读关系的探索体验。本次交付为技术文档，仓库尚无渲染代码，文中的帧率、资源预算和工期均为设计目标。

推荐方案：**TypeScript + Three.js + WebGL 2，以 Vite 构建独立渲染模块；三维实体节点、粒子星空、分层云片、局部光晕和 DOM 标签组合呈现。** 完整正文、筛选、来源与列表继续使用普通网页组件。渲染核心不绑定 React 或 Vue，后续业务框架通过适配层接入。

## 01 与产品文档的衔接

本方案承接 [产品文档 V1.1](中国历史星云宇宙_产品设计文档_V1.1.md)。根据本次明确提出的 3D 需求，将“三维显示”从原 F12 的远期项拆出，进入当前技术验证范围；全中国历史扩展仍维持原优先级。本方案以下差异覆盖原文对应的二维显示约定，其余内容、证据、时间和导航规则继续有效。

| 项目 | 本次采用的规则 |
| --- | --- |
| 桌面关系视图 | 具备能力且试点开关启用时使用 3D 星云；始终提供列表入口。 |
| 手机 | 默认摘要与列表；用户主动进入 3D，先采用轻量档。 |
| 历史实体数量 | 默认中心加8至12个关联对象，总共最多50个实体，含中心、选中与上下文节点。 |
| 装饰粒子 | 单独计算 GPU 预算，不是历史人物、事件或内容数量，不能被搜索、选中或推断为关系。 |
| 节点名称 | 3D 中按遮挡与标签预算展示，中心／选中优先；完整名称始终可从列表与详情访问。这是对原文“名称持续显示”的三维适配。 |
| 空间坐标 | 只表达排版和探索层级，不把 X/Y/Z 轴默认为时间、地理或重要程度。时间仍由底部控件表达。 |
| 动效 | 默认无自动镜头旋转、无人物漂移；用户操作产生视差。持续云气流动为可关闭的氛围选项。 |
| 分享／恢复 | 增加本机镜头与布局字段；公开分享只携带公开对象与条件，不携带私人坐标快照。 |

首个技术样本只做“一个专题、一个中心、人物与事件节点、可读关系、点击详情、展开、时间筛选、返回恢复”。体积光线步进、物理引力模拟、真实宇宙尺度、VR 和全史漫游不属于此验证范围。

## 02 目标画面与效果分层

画面采用深靛背景，远处为稀疏微亮星点，中景为低对比青蓝／紫色星云，近景为少量尘埃；人物与事件以更清楚的轮廓和暖金选中状态突出。调整镜头时，各层具有不同的透视变化；节点与关系保有真实三维坐标。

| 层 | 画面作用 | 实现 | 操作边界 |
| --- | --- | --- | --- |
| 远景星空 | 空间尺度与纵深 | 1至2批 `Points`，固定种子生成位置、亮度和大小 | 无拾取；不为每颗星创建一个 Mesh。 |
| 星云云气 | 青蓝、紫色及少量暖色的云状明暗 | 2至8张大云片，局部三维位置加面向相机的透明材质 | 位于阅读主体后方，不能遮住文字或被误认成对象。 |
| 近景尘埃 | 少量层次和视差 | 独立 `Points` 批次，较低不透明度 | 进入节点阅读区域时稀疏化，不制造密集雪花。 |
| 人物节点 | 人物身份入口 | 低面数球形核心，按类型实例化；外加局部光晕 | 同类型默认同尺寸，选中强调不表示历史重要程度。 |
| 事件节点 | 与人物形状区分 | 八面体核心及菱形屏幕标识 | 旋转后仍有事件文字／图标，不能仅依赖投影形状。 |
| 关系 | 已审核的主张与解释 | 带方向标识的细曲线，实线／虚线和文字说明 | 仅绘制数据提供的关系，不按空间距离自动连边。 |
| 标签与详情 | 阅读、键盘与触屏操作 | 普通 DOM，标签跟随三维投影 | 不进入 Bloom，不被星云遮挡。 |

首屏体验：先出现搜索、专题名和摘要，准备好图数据后直接展示稳定局部图；星云可以稍后淡入。进入页面不播放必须等待的穿越动画。首个关系视图应立即有名称和操作提示，不能只有一片装饰星空。

## 03 技术选型与依赖边界

| 模块 | 推荐选择 | 原因与限制 |
| --- | --- | --- |
| 构建与类型 | Vite + TypeScript | 用独立原型验证，业务壳后续可接现有框架；不是要求迁移已有项目。 |
| 三维渲染 | Three.js `WebGLRenderer` | 当前官方实现以 WebGL 2 为基线，不能把 WebGL 1 当作兼容后备。 |
| 镜头控制 | `OrbitControls` | 旋转、平移和缩放；手势映射按第08章显式配置。 |
| 星空／尘埃 | `BufferGeometry` + `Points` + 自定义材质 | 合并提交粒子数据，GPU 计算局部亮度变化。 |
| 节点 | `InstancedMesh`，人物／事件分别成批 | 共用几何体与材质，ID 与实例索引通过适配器映射。 |
| 关系线 | `LineSegments2` + `LineSegmentsGeometry` + `LineMaterial` | 将曲线采样为线段并按样式合批，支持可控屏幕线宽。 |
| 后处理 | Three.js addons 的 `EffectComposer`、`UnrealBloomPass`、`OutputPass` | 高档可选；基础光晕无需后处理也成立。 |
| 标签与可访问性 | DOM 投影标签、普通列表与按钮 | 控制中文清晰度、键盘焦点和点击区域。 |
| 布局 | 固定种子的有界布局函数 | 50节点先在主线程按需计算；实测超预算再迁移 Web Worker。 |

引擎事实依据：[WebGLRenderer](https://threejs.org/docs/pages/WebGLRenderer.html)、[Points](https://threejs.org/docs/pages/Points.html)、[InstancedMesh](https://threejs.org/docs/pages/InstancedMesh.html)、[LineMaterial](https://threejs.org/docs/pages/LineMaterial.html)。

本次不写未经安装验证的“最新版版本号”。建项目时锁定 Three.js 的确切版本，addons 从同一个 `three` 包导入；配套类型版本须通过类型检查，提交 lockfile。不能把不同版本的核心与 CDN 示例脚本混用。

WebGPU 可作为后续另一个后端评估，本版只实现 WebGL 2。其渲染器、材质与线条实现有差异，不把切换后端视为替换一个构造函数。React Three Fiber 仅在业务最终选择 React 时另评估，核心方案不依赖它。

## 04 模块职责与单向数据流

| 模块 | 输入 | 输出与职责 |
| --- | --- | --- |
| `GraphRepository` | 中心、时间、筛选、内容版本、请求ID | 获取已发布节点／主张和下一批关联，处理缓存与撤回状态。 |
| `ExplorationController` | 语义动作和请求结果 | 管理历史、选择、展开、筛选提交及本机快照，是业务状态唯一入口。 |
| `GraphAdapter` | 已通过校验的业务图 | 输出实体实例、线束、标签及可拾取ID；区分上下文对象。 |
| `LayoutEngine` | 稳定ID、邻接、旧位置、种子、布局版本 | 输出有界三维坐标，不决定历史关系与推荐事实。 |
| `NebulaScene` | 视图模型、坐标、质量档 | 管理星空、云片、节点、连线和渲染资源。 |
| `CameraController` | 镜头快照、居中／回正／切换中心动作 | 限制旋转和平移范围，维护镜头与 controls target。 |
| `InteractionController` | Pointer、键盘、DOM标签事件 | 区分点击和拖拽，拾取ID，再向业务层发送动作。 |
| `LabelManager` | 投影位置、遮挡、选择、可用画布区域 | 排列有限标签，保证选中对象与完整列表可访问。 |
| `QualityController` | 帧间隔、可见性、能力、用户设置 | 调整 DPR、云片、后处理与装饰粒子，不删除历史数据。 |
| `ResourceTracker` | 创建／释放事件 | 维护共享资源引用与销毁顺序，防止切专题时泄漏。 |

更新链路：用户操作 → 业务动作 → 请求与校验 → 有界布局 → 提交一份新业务／渲染模型 → 更新 GPU 数据与 DOM。渲染帧内只读已提交状态，不能每帧回写整个页面状态或重新生成关系。

画布只占一个工作台实例；详情和来源覆盖时复用该实例，来源打开后禁用画布交互。页面真正卸载才销毁资源。所有事件处理和动画入口都必须可取消。

## 05 三维布局与稳定性

### 坐标策略

采用右手坐标、Y 轴向上，世界单位不等于公里或年份。当前中心位于原点附近，初始观察方向沿 +Z 看向原点。一级关联分布在半径约40至70的区域，展开出的次级节点放到父节点周围；默认 Z 深度约为横向宽度的三分之一，既能形成视差，也避免初次阅读严重遮挡。以上参数是原型起点。

初始位置由 `layoutVersion + topicId + centerId + nodeId` 的固定种子计算，输入按稳定ID排序。只用有依据的邻接确定探索层级；不能把某次任职、阵营或生卒永久编码成空间分区。

### 布局步骤

1. 先预留中心、选中对象及选中关系两端的位置和节点预算；去重后最多50实体。
2. 对首次出现节点按层级和固定种子给初始方向；对已有节点优先复用坐标。
3. 执行有限次数的排斥、碰撞分离和轻量连边约束。中心固定，旧节点施加强锚定，新增节点承担主要移动。
4. 约束位置回到可用空间，输出包围球和坐标表。循环必须有迭代上限，不能永久运行物理模拟。
5. 小幅移动可在200至350ms内插值完成；减少动态效果开启时直接使用终态。插值坐标同时供节点、关系、标签和拾取使用。

同一输入在固定浏览器／算法版本下应稳定；跨设备浮点细节不承诺像素完全一致。本机返回优先恢复存储坐标，公共链接只恢复条件后重新构图。

### 各动作对布局的影响

| 动作 | 布局变化 |
| --- | --- |
| 选中／悬停 | 不重排；仅改变高亮、标签与摘要。 |
| 展开 | 保留原布局，新增节点在局部收敛；失败不追加半批节点。 |
| 成功应用时间／筛选 | 回到当前中心的新默认邻接集合；复用仍存在节点的位置，原完整图进入历史快照。 |
| 以此为中心 | 创建新的中心布局并保存旧图；选择本身不触发这一行为。 |
| 撤销展开 | 恢复展开前节点、边、位置、选择和镜头。 |
| 重置图谱 | 当前中心／条件下重新生成默认图，清空展开撤销栈，可通过历史返回。 |

先用50节点样本检测布局耗时，目标 p95 小于20ms。若不满足，则改用 Worker 或分片计算；消息需携带 `viewRevision`，旧结果丢弃。取消旧任务不仅依赖网络 Abort，也要阻止旧布局回写。

## 06 星空、云片、光晕与后处理

### 粒子星空

每颗装饰星保存位置、大小、亮度、相位和色彩索引；位置只在初始化或换主题时生成。用少量球壳／空间层形成远近关系，粒子均不进入实体ID映射和射线拾取层。

典型粒子可压成约7个 Float32 属性，8,000颗星的原始属性数据约219KiB；这不是整个场景显存，仅为该属性数组估算。与其按帧上传全部坐标，优先只更新 `uTime`、相机和少量状态。大光晕用面向相机的四边形，避免依赖设备支持任意大的 `gl_PointSize`。

默认星空静止，镜头操作本身已产生三维视差。开启“氛围流动”时只对装饰层使用缓慢、低幅度变化，不移动人物和事件。不开流动时 `uTime` 停止推进；返回页面不补跑隐藏期间的动画。

### 云片方案

云片是位于不同三维位置的柔边四边形，使用共享的小型噪声纹理／径向遮罩与不同缩放、旋转、颜色和深度。建议初始使用256×256噪声纹理，最多2至3层采样叠加，先用静态纹理保证效果，再按需增加缓慢 UV 漂移。

材质逻辑示意，属于算法说明而非完整可运行 GLSL：

```text
noise = 0.60 * sampleNoise(uv)
      + 0.28 * sampleNoise(uv * 2.1 + offsetA)
      + 0.12 * sampleNoise(uv * 4.0 + offsetB)
mask = radialFalloff(uv) * smoothThreshold(noise)
alpha = mask * layerOpacity
color = mix(coolBlue, violet, noise)
```

噪声数据纹理不按颜色纹理做 sRGB 解码。云片采用 `depthWrite=false`；先渲染装饰背景，再画业务实体，减少透明排序影响。相机旋转时云片保持面向镜头并改变相对投影，不要求其具备物理真实的气体体积。

背景云片应保持低对比，优先减小覆盖面积和重叠数。禁止通过堆叠几十张全屏透明面片增加“浓度”，因为透明像素重复计算会快速吃满移动端填充率。背景与业务分层时使用独立场景／绘制阶段，并在绘制业务实体前清除深度；因此云气是氛围背景，不承担物体之间的真实遮挡关系。

### 节点光晕

节点核心使用不透明、无实时阴影的材质；外层光晕使用合批的面向相机四边形，透明叠加且不写深度。普通节点光晕弱，选中／悬停增强；环形描边和文字状态共同提示选择。基础光晕必须在 Bloom 关闭时仍清楚。

### 可选 Bloom 与颜色处理

高档增加克制的泛光，示例起点 `strength=0.25`、`radius=0.35`、`threshold=1.1`，仅供调参。星空和云气保持阈值以下，选中核心可输出较高线性亮度。控制值需随实际颜色管理和渲染目标格式验证，不承诺一套参数覆盖所有设备。

统一流水线：背景阶段 → 清深度 → 实体／关系／光晕阶段 → 可选 Bloom → 输出颜色转换 → DOM 标签与页面。Composer 里需实现一个负责前两场景合成的场景 Pass；不能使用默认清屏的第二个 RenderPass 把背景抹掉。

使用后处理时由 `OutputPass` 负责末端色调映射与颜色转换；建议显式配置 `renderer.toneMapping = ACESFilmicToneMapping`、输出为 `SRGBColorSpace`，无后处理时走经过对应输出处理的直接渲染路径。自定义 ShaderMaterial 必须配合当前管线，避免先在片元做颜色转换再被输出层重复转换。DOM 文字不进后处理。若后续加需要sRGB输入的FXAA，应放在OutputPass之后；本版不依赖该Pass。对应机制见 [EffectComposer](https://threejs.org/docs/pages/EffectComposer.html)、[OutputPass](https://threejs.org/docs/pages/OutputPass.html)、[UnrealBloomPass](https://threejs.org/docs/pages/UnrealBloomPass.html)。

统一颜色约定：材质颜色、实例／顶点RGB数组使用Linear-sRGB；PNG／JPEG等颜色贴图标记 `SRGBColorSpace`，噪声与透明度遮罩使用 `NoColorSpace`。不要把直接读取的CSS RGB数值原样写入顶点数组，也不要用增加亮度掩盖重复转换。依据：[Three.js 颜色管理](https://threejs.org/manual/pages/color-management.html)。

HDR 渲染目标及相关能力不足、创建失败或性能不达标时关闭 Bloom，保留基础光晕；不因后处理失败让整个星云不可用。首版不叠景深、运动模糊、色散、实时阴影或反射探针。

## 07 实体、关系、标签与拾取

### 实体实例

人物球体与事件八面体分别创建 `InstancedMesh`，维护 `instanceIndex → entityId` 和反向索引。筛选后的重新压缩索引不能改变业务选择，选择始终存稳定ID。修改实例矩阵／颜色后标记相应属性 `needsUpdate`，位置变化后重新检查包围球，避免错误剔除。相关 API 约束见 [InstancedMesh](https://threejs.org/docs/pages/InstancedMesh.html)。

人物或事件不做仅在顶点着色器中存在、CPU 无法感知的位置漂移。50节点无需 GPU 拾取：射线拾取实体 Mesh，再结合屏幕距离扩展小目标命中范围。装饰层、不可见节点与被筛掉的关系不得进入命中候选。[Raycaster](https://threejs.org/docs/pages/Raycaster.html) 支持按层过滤，并可返回实例索引。

### 关系曲线

每条主张必须保留关系ID、两端、方向、材料类别和时间匹配状态。同两端多条主张可按“两端、方向类别、记载／解释类别”形成线束；点击后列出具体主张。记载与解释不能被合为无法辨别的一条线。

使用轻弯二次曲线，普通档每条约8段，高档最多16段；相同两端的曲率方向由稳定ID决定，避免每次刷新跳位。优先按实线、虚线、高亮和上下文等少量样式合批为 `LineSegments2`，不为每条曲线创建一个独立材质。方向用少量实例化箭头表示，不用持续流动暗示因果方向。

虚线需维护弧长，避免每小段从零开始出现接缝；样式或曲线改变后重算距离数据。`LineMaterial` 使用 `worldUnits=false`，线宽按屏幕尺寸定义；当前可见对象会在渲染前更新viewport resolution，多视口／后处理目标仍需验证，不能固定写窗口分辨率。不要通过 `LineBasicMaterial.linewidth` 假定各平台都会显示粗线。依据：[LineMaterial](https://threejs.org/docs/pages/LineMaterial.html)、[LineSegments2](https://threejs.org/docs/pages/LineSegments2.html)。

首个50实体压力样本包含最多240条主张。正常情况下展示这些主张的线束；超过240条的后续数据进入分批关系列表，画布显示已加载数量和剩余数量，不静默截断。可读性过低时由用户选择“仅看当前对象关联”，这是显式视图筛选，不能被性能降级偷偷启用。

240是单次画布加载的主张预算，不是后台收录上限；当前有效的选中主张优先占用名额。服务返回的画布数据须同时满足实体上限与端点完整性；超额或缺端点视为协议错误，保留原图并进入恢复流程，不能任意丢掉一个中心或端点来凑数。更多关联列表的数据不直接追加到GPU图，须经展开／切中心动作重新校验预算。

关系点击采用曲线采样点投影后的屏幕距离检测，拾取宽度独立于视觉线宽；交叠时显示候选主张菜单。默认优先命中节点、再命中关系；射线深度与屏幕距离共同决定候选，不能点到被实体完全挡住的后方对象。对于近裁剪面穿越的曲线，先裁剪可见段，不能直接投影不可见点形成贯穿屏幕的命中线。

### 标签与可访问性

中文名称用 DOM 标签，随最终渲染坐标投影。中心、选中对象、关系两端、键盘焦点优先；其他标签按屏幕距离和可用空间布局。手机初始预算12个，桌面24个；这些是同时显示标签的数量，不是已收录或可浏览对象数量。

标签碰撞用屏幕矩形检测，优先尝试上下左右偏移并绘制短引导线，仍冲突则隐藏低优先级标签。视锥外、相机后方和被完全遮挡的普通节点不显示标签。选中对象在屏外时显示“当前选中”固定提示与定位按钮，不把其标签钉到另一个节点上。

DOM 更新只在镜头、布局、选择或容器尺寸变化时发生；动画期间更新当前可见标签。标签文字通过安全文本渲染，不直接注入外部HTML。重名显示补充年代／身份。触屏提供至少44px目标范围，重叠目标弹出候选，列表保留完整且可键盘访问的名称与关系。

## 08 镜头与手势规则

使用 `PerspectiveCamera`，建议初始垂直 FOV 45°；near/far 和缩放界限根据局部图包围范围设置，避免近到穿过核心或远到看不清全部实体。默认限制极角约25°至155°，保持 Y 轴向上，不提供翻滚。

`OrbitControls.target` 是镜头围绕的观察点，`centerId` 是业务中心，两者不能混为一项。平移可移动观察点，不能改变业务中心。当前官方 Controls 支持旋转、平移、缩放与触控映射；启用阻尼时需要在动画帧更新，见 [OrbitControls](https://threejs.org/docs/pages/OrbitControls.html)。

| 输入／按钮 | 桌面 | 手机 | 业务结果 |
| --- | --- | --- | --- |
| 点击／轻点节点 | 选中 | 选中 | 更新摘要，不移动中心和镜头。 |
| 拖动画布 | 左键旋转，右键平移 | 默认单指平移 | 只改镜头；手机点击“旋转模式”后单指才旋转。 |
| 缩放 | 滚轮 | 双指缩放 | 有最小／最大距离限制。 |
| 双指拖动 | 不适用 | 平移，禁止同一手势额外触发单指旋转 | 不触发点击。 |
| 定位当前选择 | 保留对象与筛选，调整观察点和距离 | 同桌面 | 只改镜头，不换业务中心。 |
| 居中全部 | 适配当前实体包围范围 | 同桌面 | 保持当前朝向，不重排对象。 |
| 回正视角 | 恢复默认朝向并适配当前图 | 同桌面 | 只改镜头；与“重置图谱”分开。 |
| 以此为中心 | 保存旧快照，载入新中心局部图 | 同桌面 | 新业务历史记录。 |

`controls.touches.ONE` 默认映射 PAN，TWO 映射 DOLLY_PAN；旋转模式将 ONE 改为 ROTATE。这是主动配置，不采用 OrbitControls 默认单指旋转。手机3D区域使用独立触控容器；仅该区域接管手势，详情页面仍能正常滚动，不能全页面 `preventDefault`。

Pointer down/up 位移超过建议6 CSS px（鼠标）／10 CSS px（触屏）即认定拖拽，或出现第二触点时取消点击候选；`pointercancel` 清理状态。控件拖拽结束不能误选节点。节点本身不开放拖动改位置，避免同旋转冲突。

定位动画建议350至600ms，由用户明确触发；输入打断时以当前位置结束。恢复历史快照直接还原镜头，减少动态效果模式下所有定位直接到终态。自动镜头旋转默认关闭，不用选中动作触发飞行。

居中按可用画布区域计算包围球投影，考虑侧栏、底部面板和标签留白。跨屏幕尺寸恢复时保持观察方向、中心和相对距离，再重新约束可见范围；不能生搬桌面像素。布局、拾取和 DOM 标签都使用画布容器的实际矩形，而非整个窗口尺寸。

## 09 数据契约、状态与恢复

以下 TypeScript 是接口设计，不表示已实现服务。历史时间匹配由内容／查询层完成，渲染器只消费结论；不得在 Shader 中用“生卒相交”自行创建关系。

```ts
type Vec3 = [number, number, number];
type TimeQuery =
  | { mode: 'year'; year: number }
  | { mode: 'range'; fromYear: number; toYear: number };
type MatchState = 'certain' | 'possible' | 'undated' | 'context';

interface EntityView {
  id: string;
  kind: 'person' | 'event';
  label: string;
  subtitle?: string;
  match: MatchState;
}

interface ClaimView {
  id: string;
  sourceId: string;
  targetId: string;
  type: string;
  direction: 'forward' | 'bidirectional' | 'none';
  evidence: 'record' | 'interpretation';
  match: MatchState;
  timeLabel: string;
  sourceRefIds: string[];
}

interface GraphResponse {
  requestId: string;
  queryKey: string;
  contentVersion: string;
  nodes: EntityView[];
  claims: ClaimView[];
  remainingNodeCount: number;
  remainingClaimCount: number;
  nextCursor?: string;
}

interface CameraSnapshot {
  position: Vec3;
  target: Vec3;
  up: Vec3;
  fov: number;
  aspectAtSave: number;
}

interface SpatialSnapshot {
  schemaVersion: 1;
  layoutVersion: string;
  layoutSeed: string;
  contentVersion: string;
  positions: Record<string, Vec3>;
  camera: CameraSnapshot;
  viewMode: 'graph3d' | 'list';
}
```

`SpatialSnapshot` 是 V1.1 已定义探索快照的扩展，外层继续保存专题、中心、选择、关系详情层、时间、筛选和展开集合。没有3D字段的旧快照迁移为默认镜头和稳定布局。设备画质与是否开启氛围流动属于本机偏好，不是历史业务状态，不分享、不随浏览器后退跳变。

运行时另存 `preferredView`、`effectiveView`、`qualityTier`、`fallbackReason` 和 `requestGeneration`：例如用户偏好3D但context丢失时，实际视图变列表，不能把恢复用的3D坐标清空。透视相机固定 `zoom=1`，缩放只改变相机距离；将来允许修改zoom时，须升级镜头快照字段。

约束：所有坐标与相机值须有限且在合理边界内；中心、选中、关系两端均须为已发布且可用对象；关系端点齐全后才能画线；`context` 关系可读详情但不能画为当前有效边；可能相关／待考仅在对应分组开启时画出并保留标签。客户端类型不能代替运行时数据校验。

### 建议服务接口

| 接口职责 | 必要参数 | 结果 |
| --- | --- | --- |
| 获取局部图 | 专题、中心、时间、关系类型、日期分组、节点预算 | 默认节点和关系、查询键、内容版本、剩余数量。 |
| 展开邻接 | 局部图条件、展开对象、已显示ID、剩余容量、分页游标 | 可新增节点／关系和剩余数量；同ID去重。 |
| 获取主张详情 | 主张ID、已知版本 | 具体时间、情境、出处、其他说法、发布状态。 |
| 校验探索快照 | 内容版本、实体／主张ID | 当前可用、变更、撤回集合；不上传私人镜头和阅读位置。 |

实际 HTTP 路由可在服务设计时确定，本方案不假称已有 API。首个原型使用本地 JSON 适配同一契约；测试数据须明确为审核样本或合成数据，合成关系不能以历史事实发布。

### 筛选与异步提交

点击应用后生成新的 `requestId/viewRevision`；保留上次成功业务状态和镜头，新条件显示“正在更新”。准备并校验结果、计算布局后，只允许最新有效请求一次提交数据、条件、视图和一条历史记录。旧网络结果、旧布局结果和旧动画回调都应失效。

失败保留原图与原展开撤销栈，失败条件回到草稿；返回、切中心或切换专题均取消待处理任务。镜头对象、DOM 标签和实例索引映射与新图同时更新，避免标题属于B、选中的实例仍属于A。

本机保存坐标表和镜头；公开分享仅用 V1.1 字段白名单，并允许 `view=graph3d` 作为偏好。接收者按当前内容与设备能力构图；手机仍默认列表，且不承诺复现原镜头、展开布局或画质。后退／前进恢复业务和空间快照，控件镜头变动只更新当前快照，不逐帧新增历史。

## 10 渲染调度与性能分级

### 循环策略

画布只保留一个调度入口，使用 Three.js 动画循环接口统一管理。用户拖拽、阻尼未收敛、节点插值、数据变更或开启氛围流动时需要连续帧；其余状态绘制一次后停止循环。页面隐藏或3D切到列表时停止整个动画调度与拾取，氛围开关不能阻止暂停；恢复时重置时间并触发一次 `invalidate()`，仅按当前快照绘制，不补帧。详情长阅读但画布仍可见时可只暂停装饰更新；来源层打开时另按交互规则禁用底层操作。

启用阻尼时不能在 pointer up 当刻停循环，应继续更新到相机变化低于阈值。恢复循环需要重置上一帧时间并限制 `deltaTime`，防止切回标签页发生镜头大跳。外部 `invalidate()` 只安排一次唤醒，避免事件叠加生成多个 RAF／动画循环。

### 初始质量预算

| 档位 | 使用条件 | 星空／尘埃上限 | 云片 | DPR上限／绘制像素上限 | 后处理 | 交互目标 |
| --- | --- | --- | --- | --- | --- | --- |
| Q0 | WebGL2失败、用户选择列表、不可恢复错误 | 不绘制3D | 不绘制 | 首次列表不创建；已有实例暂停保留 | 不执行 | 普通DOM列表与详情可用；故障重建按第11章处理。 |
| Q1 | 手机初次3D、弱设备 | 1,500／300 | 2 | 1.0／0.8MP | 关闭Bloom | 目标30FPS，帧间隔p95≤40ms。 |
| Q2 | 桌面默认 | 4,000／1,000 | 5 | 1.25／1.5MP | 基础光晕 | 目标60FPS，帧间隔p95≤25ms。 |
| Q3 | 明确开启高画质且实测可承受 | 8,000／2,500 | 8 | 1.5／2.1MP | 可选Bloom | 目标60FPS，帧间隔p95≤25ms。 |

以上各档均支持50实体和相同历史关系数据，不能因降档减少内容或隐藏正在阅读的对象。标签数量按屏幕可读性而非GPU档位控制。帧目标针对持续交互阶段，静止按需渲染时不以低“平均FPS”判定失败。

实际 DPR 计算为 `min(devicePixelRatio, tierDprCap, sqrt(pixelBudget / (cssWidth * cssHeight)))`，大屏可能低于1。CSS文字仍按浏览器正常像素显示。ResizeObserver 通知后，统一更新 camera aspect、renderer尺寸、composer尺寸、线宽resolution和标签矩形；DPR只乘一次，防止重复放大离屏目标。

### 自动降级与诊断

预热完成后，以持续交互的3秒窗口统计帧间隔；连续两个窗口超过当前档位阈值时降一级，设15秒冷却，避免抖动。只用CPU时间不能判断GPU完成；初版以用户可感知帧间隔、长任务与资源计数综合判断。隐藏页面、首屏编译和按需空闲不计入该窗口。

优先关闭 Bloom、减小绘制分辨率，再减少云片和装饰粒子。Q1连续失败时提示切列表并自动保留视图；返回3D由用户触发重试。本次会话不自动升档，用户可选择试用高画质，防止恢复后立即反复降升。探测能力与扩展只在初始化或重建时进行，不在每帧调用可能导致阻塞的查询。依据：[MDN WebGL 性能实践](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)。

### 性能测量与资源上限

| 指标 | 试点目标及测量口径 |
| --- | --- |
| 实体点击反馈 | 已加载50实体图，pointer up 至高亮／加载反馈 p95≤200ms；不包含远程详情完成。 |
| 首次可交互图 | 10Mbps、100ms往返、冷缓存；进入工作台至可选中默认节点 p75≤4秒。普通详情首屏仍沿用产品3秒目标。 |
| 布局 | 固定50实体样本，p95≤20ms；超预算才启用Worker／分片。 |
| Draw calls | Q1/Q2全帧目标≤60，Q3含Bloom各Pass≤100；用全帧累计统计验证。 |
| 下载体积 | 懒加载3D JS压缩后目标≤800KiB，初始装饰纹理压缩后≤500KiB；具体以构建报告为准。 |
| GPU资源估算 | Q1≤32MiB，Q2≤64MiB，Q3≤96MiB；统计应用自己分配的Buffer、Texture和RenderTarget，不假称可读取全机显存。 |
| 长时稳定性 | 连续10分钟交互与20次进入／退出图谱后，活动资源计数回归稳定基线，无持续增长。 |

例如1280×720在DPR1.5时约207万绘制像素，两张RGBA16F颜色缓冲就约31.6MiB，尚未包括深度与Bloom中间纹理。因此控制绘制分辨率和全屏Pass，通常比单纯减少几百颗星更重要。资源估算方法及像素预算依据见 [MDN WebGL 性能实践](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)。

`renderer.info` 用于观察对象与绘制计数，不直接等于字节显存；多Pass时关闭自动逐次重置，在整帧末读取累计值再重置，避免只看到最后一个Pass。API语义见 [WebGLRenderer](https://threejs.org/docs/pages/WebGLRenderer.html)。

## 11 兼容、降级与资源释放

进入3D时实际尝试创建 WebGL2 renderer；失败则显示同条件列表与明确提示。不能仅凭浏览器名称、UA或屏幕宽度判断GPU能力。首轮测试覆盖桌面Chrome／Edge／Firefox／Safari及Android Chrome、iOS Safari，记录确切版本、设备与分辨率后形成支持矩阵；本方案不声称已经通过这些设备。

| 故障／状态 | 处理 |
| --- | --- |
| Shader或纹理加载失败 | 先降到基础材质和静态背景；业务节点可读，局部提供重试。 |
| 半浮点目标／Bloom初始化失败 | 关闭后处理，重建基础输出链，不丢失当前选择。 |
| WebGL context lost | 停止循环与拾取，保存业务／CPU快照，展示列表和恢复状态。 |
| context restored | 从保留的CPU数据和材质定义重建／重新上传资源，校验内容版本，恢复低一档视图。 |
| 30秒内未恢复或同会话再次丢失 | 保持列表，提供用户触发的重建；禁止无限自动重试。 |
| 网络失败 | 保留已提交内容；新请求失败不能伪装成无关系。 |
| 减少动态效果 | 关闭氛围流动与镜头插值，3D交互仍可直接响应；保留列表。 |

context事件直接监听在canvas，恢复策略下对丢失事件调用 `event.preventDefault()`；Three.js当前内部监听也有对应处理，业务层仍负责暂停循环与恢复状态。恢复不等于旧GPU句柄重新有效。相关生命周期见 [context lost](https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/webglcontextlost_event)、[context restored](https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/webglcontextrestored_event) 及 [Three.js渲染器源码](https://github.com/mrdoob/three.js/blob/dev/src/renderers/WebGLRenderer.js)。Three.js自身管理的资源与自定义Pass资源需要分别验证，不能复用缓存的原生WebGL句柄。

卸载顺序：取消请求与布局任务 → 停止动画与定时任务 → 移除控件／Pointer／Observer监听 → 清理标签DOM → 释放独占geometry、material、texture和render target → dispose各Pass、composer、controls和renderer。移出Scene不等于释放资源；共享几何体与纹理由引用计数管理，不能因删除一个节点销毁所有节点共用资源。

释放材质不会自动释放它使用的纹理；清理composer也不应代替逐个清理用户创建的Pass。按所有权执行释放，计数稳定不要求引擎内部缓存立刻变成零。资源生命周期依据：[Three.js资源释放手册](https://threejs.org/manual/pages/how-to-dispose-of-objects.html)。

清理属于幂等操作；重复mount/unmount、初始化中失败和context丢失时也可安全调用。不得在切换详情时反复创建新的WebGL上下文。

## 12 实现目录与资产约定

建议初始目录如下，均为待创建模块，本次未生成这些代码：

```text
src/
  domain/              # 历史实体、主张、时间查询与校验
  exploration/         # 业务状态、历史、快照与数据仓库
  nebula/
    scene/             # 背景、节点、关系、光晕与组合Pass
    layout/            # 固定种子、局部布局、可选Worker
    camera/            # 控制、镜头快照、居中与回正
    interaction/       # 拾取、拖拽判定、候选关系
    labels/            # DOM投影、碰撞与焦点
    shaders/           # 星点、云片、光晕
    quality/           # 档位、帧统计与降级
    lifecycle/         # 资源追踪、恢复与销毁
  ui/                  # 搜索、筛选、摘要、来源、列表
  fixtures/            # 审核样本与明确标注的合成测试数据
```

技术原型使用程序生成噪声和光晕，不要求购买星空素材或加载人物3D模型。所有生成随机效果固定种子；若后续使用图片，记录来源、授权、分辨率与压缩体积。噪声／遮罩与颜色纹理分开管理色彩空间。

调参集中放配置：各档粒子数量、云片不透明度、色板、DPR、Bloom、线宽、标签预算、镜头限制与布局参数。调试面板仅开发环境启用；生产界面只提供用户能理解的画质、氛围动效、旋转模式、定位和列表选项。

## 13 验收场景

继承产品文档 AC01至AC26，另增加以下3D专项。全部为待执行测试，文档检查不代表性能或效果达标。

| 编号 | 测试场景 | 通过条件 |
| --- | --- | --- |
| T3D01 | 旋转／缩放默认专题图 | 节点有真实透视与深度变化，云片与星空形成层次；实体与标签可辨认。 |
| T3D02 | 选中、展开、切换中心分别操作 | 只有切换中心改变业务中心；拖拽镜头不误选，装饰星点不响应点击。 |
| T3D03 | 50实体、240主张、各档装饰粒子 | 满足档位帧间隔和全帧draw预算；降档不减少历史数据。 |
| T3D04 | 展开后转动镜头、打开详情再返回 | 节点坐标、选择、观察点与镜头恢复；返回不重新随机布局。 |
| T3D05 | 时间筛选让正在读的关系失效 | 连线退出有效网络，详情仍标明原有效期和失配；不把Z轴当时间。 |
| T3D06 | 新筛选失败、旧网络／布局结果随后到达 | 原图和撤销栈保留，失败不入历史，旧结果不能覆盖。 |
| T3D07 | 手机单双指操作并快速切回详情 | 默认单指平移、双指缩放／平移；旋转模式明确；页面正文可正常滚动。 |
| T3D08 | 相机绕到节点背面、节点重叠、关系交叉 | 标签不贴错对象，遮挡及候选菜单正确，隐藏对象不被错误拾取。 |
| T3D09 | 更改窗口、DPR和侧栏宽度 | 镜头比例、DOM投影、线宽和点击命中对齐，绘制像素不超预算。 |
| T3D10 | 在列表与3D之间切换 | 业务中心、条件、选中一致，3D镜头可恢复，无重复上下文和循环。 |
| T3D11 | 强制WebGL不可用、Shader失败、禁用HDR目标 | 同条件列表或基础图仍能完成阅读，错误不显示成空关系。 |
| T3D12 | 模拟context丢失、恢复与二次丢失 | 停止旧渲染，资源重建后恢复正确状态；重复失败保留列表。 |
| T3D13 | 后台标签页30秒，再返回；减少动态效果开启 | 后台停止渲染，恢复无大步跳变；无持续人物漂移或强制飞行。 |
| T3D14 | 同一专题进入／退出20次、操作10分钟 | 内存与资源计数在稳定基线附近，无事件、Worker、循环持续累积。 |
| T3D15 | 无历史浏览器打开3D分享链接 | 同公开条件和有效关系，按设备适配；无私人坐标、轨迹与用户标识。 |
| T3D16 | 键盘或只用列表完成探索闭环 | 能找到行动、关系与来源；不要求精确点中三维细线。 |
| T3D17 | 切换Bloom与无Bloom输出，检查中文标签 | 色彩无重复Gamma或明显跳变，标签清楚，关闭Bloom仍有选择反馈。 |
| T3D18 | 旧2D快照迁移，保存中的主张被撤回 | 可用状态迁移，失效内容明确移除，无空白页或幽灵连线。 |

性能录制统一使用固定数据、种子、视口和交互脚本；每档预热后采集3次、每次30秒旋转／缩放／选择，另测冷启动与10分钟稳定性。记录设备、系统、浏览器版本、实际绘制尺寸、质量档和内容版本。真实设备GPU是性能门槛，软件渲染或无头浏览器只用于逻辑回归。

## 14 实施顺序与交付门槛

以下为3D前端专项估算，假设一名熟悉Three.js的前端、设计支持和可用样本数据；不包含整个产品后台、史料整理和完整上线。建议约15个工作日，首阶段实测后重估。

| 阶段 | 工作 | 可评审结果 |
| --- | --- | --- |
| 第1至3日 | 搭建渲染壳、稳定节点布局、基础星空／云片、实例拾取 | 真实3D局部图，点击可识别对象；有无WebGL两条路径。 |
| 第4至6日 | 关系合批、方向与虚线、标签、镜头和触屏 | 人物／事件／关系可读，旋转和点击无冲突。 |
| 第7至10日 | 时间条件、展开、历史／空间快照、列表与来源对接 | 完成产品闭环，不因3D丢失原交互语义。 |
| 第11至13日 | 画质档、可选Bloom、按需渲染、资源恢复与释放 | 固定样本性能报告、错误降级和稳定性报告。 |
| 第14至15日 | 手机实测、专项验收、视觉调参和交付整理 | 源码、锁定依赖、参数配置、测试记录及运行说明。 |

先让无Bloom的Q1/Q2满足阅读与交互，再决定Q3是否开放。首发用单独的 `enableNebula3D` 开关控制；关闭开关立即回到同条件列表，不能影响搜索、完整详情或来源服务。不开全史数据与三维无边界漫游作为附带承诺。

## 15 参考资料与核对范围

技术机制于2026年9月24日查阅以下官方资料；这些资料支持API与行为说明，文中的视觉参数、预算和进度由本方案提出，需原型验证。

- [Three.js WebGLRenderer](https://threejs.org/docs/pages/WebGLRenderer.html)：WebGL2基线、尺寸、输出、编译、统计和释放。
- [Three.js OrbitControls](https://threejs.org/docs/pages/OrbitControls.html)：目标点、手势、限制、阻尼和状态。
- [Three.js Points](https://threejs.org/docs/pages/Points.html)、[InstancedMesh](https://threejs.org/docs/pages/InstancedMesh.html)、[Raycaster](https://threejs.org/docs/pages/Raycaster.html)：粒子、实例与拾取。
- [Three.js LineSegments2](https://threejs.org/docs/pages/LineSegments2.html)、[LineSegmentsGeometry](https://threejs.org/docs/pages/LineSegmentsGeometry.html)、[LineMaterial](https://threejs.org/docs/pages/LineMaterial.html)：批量宽线与虚线。
- [Three.js EffectComposer](https://threejs.org/docs/pages/EffectComposer.html)、[UnrealBloomPass](https://threejs.org/docs/pages/UnrealBloomPass.html)、[OutputPass](https://threejs.org/docs/pages/OutputPass.html)：后处理与末端色彩转换。
- [Three.js颜色管理](https://threejs.org/manual/pages/color-management.html)、[资源释放](https://threejs.org/manual/pages/how-to-dispose-of-objects.html)：颜色空间、共享资源与释放责任。
- [MDN WebGL 性能实践](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)：能力、绘制分辨率、资源与避免阻塞。
- [MDN context lost](https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/webglcontextlost_event)、[context restored](https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/webglcontextrestored_event)：上下文生命周期。

历史事实与内容审核仍以产品文档及内容团队复核为准；三维布局、云气颜色和视觉距离不构成历史证据。
