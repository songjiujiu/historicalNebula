# 《史记》接入技术说明

本次接入的是《史记》中的**楚汉之际**样本，时间范围为公元前 209—202 年，以鸿门宴为默认探索中心。它与既有“汉末 · 三国 / 赤壁”专题分开：赤壁发生在《史记》成书之后，不能把《史记》标成赤壁关系的来源。当前实现仍是无需后端的本地内容原型，不代表《史记》130 卷已完成结构化录入或史料审校。

## 数据与专题边界

- `src/domain/topics.ts` 定义专题 ID、标题、时间范围、默认中心、时间刻度与导览入口。现有 ID 为 `three-kingdoms` 和 `shiji`。
- `src/domain/shiji-data.ts` 单独保存楚汉样本的人物、事件、行动、关系、来源及导览；`src/domain/data.ts` 汇总两个专题供 UI 使用。
- 新样本的实体、关系、来源和导览明确写入 `topicId: 'shiji'`。旧赤壁数据没有专题字段，读取时按 `three-kingdoms` 处理，保证已保存的赤壁视图可恢复。
- 图谱按专题筛选关系与端点；状态校验限制中心、选中对象、关系、详情、来源、展开对象和空间快照均属于当前专题。搜索可找到两个专题的姓名及别名，进入结果时应切到其所属专题。

现有楚汉样本为 10 位人物、6 个事件、28 条关系、7 项来源及 2 个导览。人物的 `start` / `end` 是**本专题收录的活动范围**，不是生卒年。行动属于具体事件和年份；同一行动对象在人物与事件视图复用，避免两处叙述不一致。

## 公元前纪年与状态

数据层用负整数表达公元前年份：`-209` 表示公元前 209 年，`-202` 表示公元前 202 年。升序仍是历史时间顺序，因此时间重叠与时间轴位置可以按数字比较；界面使用 `formatYear` / `formatYearRange` 输出“公元前 206 年”等文案。当前两个专题都不跨越公元 1 年，未来若接入跨公元前后的长时段，应先明确无公元 0 年的换算规则。

分享链接写入 `topic`、中心、公开的选择状态与时间筛选。旧链接若没有 `topic`，按有效的中心实体推断专题；已有赤壁链接仍可打开。浏览器本机收藏、已保存视图与导览进度只在本机保存，分享链接不包含这些私人状态，也不包含相机位置。

## 史料来源与解释层

《史记》样本按篇章建立来源 ID，而不是把“《史记》”当作一条无法定位的总引用。当前原文入口为维基文库转录：

| 来源 ID | 篇章与核对入口 | 样本用途 |
| --- | --- | --- |
| `sj-007` | [卷七·项羽本纪](https://zh.wikisource.org/wiki/史記/卷007) | 巨鹿、鸿门、彭城、垓下的项羽方叙述 |
| `sj-008` | [卷八·高祖本纪](https://zh.wikisource.org/wiki/史記/卷008) | 刘邦方的会见与战事叙述 |
| `sj-016` | [卷十六·秦楚之际月表](https://zh.wikisource.org/wiki/史記/卷016) | 关键事件的纪年线索 |
| `sj-048` | [卷四十八·陈涉世家](https://zh.wikisource.org/wiki/史記/卷048) | 陈胜、吴广大泽乡起事 |
| `sj-055` | [卷五十五·留侯世家](https://zh.wikisource.org/wiki/史記/卷055) | 张良的行动 |
| `sj-092` | [卷九十二·淮阴侯列传](https://zh.wikisource.org/wiki/史記/卷092) | 韩信受拜大将、萧何推荐 |
| `sj-095` | [卷九十五·樊郦滕灌列传](https://zh.wikisource.org/wiki/史記/卷095) | 樊哙在鸿门宴的行动 |

实体、行动和关系分别保存 `sourceIds`。关系的 `evidence: 'record'` 表示该具体行动可回到所列篇章核对；`evidence: 'interpretation'` 表示编辑层的阅读线索，界面应明确区分。比如“鸿门会见”和“彭城交战”是不同年份的关系，不能用单条永久关系概括。来源页面是便于核对的公开转录，**不是已确认的正式底本**。正式发布前须逐条核对原文段落、异文、历法与公元前年份换算，并请独立内容审校。

## 扩展步骤

1. 选定《史记》中的有界时间与问题，新增可核对的篇章来源 ID，并记录每条主张所依据的卷次与段落。
2. 为人物与事件建立稳定 ID 和专题活动范围；将人物行动关联到事件，避免以阵营或同时代身份自动生成关系。
3. 给每条关系填写时间、证据类型、上下文及来源。无法确认日期时使用待考标记，不能把空日期解释为永久有效。
4. 扩充导览与搜索别名，运行 `npm test`、`npm run build`，检查跨专题引用和公元前链接往返，再在桌面及手机视口核对图谱、时间轴、原文入口和返回路径。

三维星云仍由 Three.js 实时生成。本次数据接入没有 Blender 工程、GLB 模型或烘焙贴图；视觉资产制作可独立推进，不影响来源与关系模型。
