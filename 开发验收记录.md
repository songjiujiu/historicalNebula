# 历史星云 · 首个可运行版本验收记录

首次验证日期：2026-09-24。下方“交付内容”至“后续验收范围”保留赤壁单专题阶段的检查记录；文末另记《史记》楚汉专题接入范围。项目以产品文档 V1.1 和 3D 技术方案 V1.0 为实现依据。

## 交付内容

已建立 TypeScript、Vite、Three.js 项目及锁定依赖，完成程序生成的三维星云、汉末三国样本、人物与事件阅读、来源入口、时间和关系筛选、专题导览、本机记录与公开链接。构建产物输出到 `dist/`，可用普通静态 HTTP 服务运行。

## 自动检查

- `npm run build`：TypeScript 检查与生产构建通过。
- `npm test`：2 个文件、23 项测试通过，覆盖图谱预算、时间边界、待考关系、状态校验、简繁体与别名搜索、分享字段白名单、完整详情 URL 往返及数据引用一致性。
- Three.js 独立分包并在进入 3D 时加载。其压缩后文件约 594 kB，gzip 约 148 kB，构建仍提示单包超过默认 500 kB 阈值；这是后续体积优化项，不是构建失败。

## 浏览器交互检查

在 Codex 内置浏览器使用本地开发服务器验证；桌面视口约 1280 × 720，手机模拟视口 390 × 844。以下为实际 UI 操作结果，不代表实体手机或多浏览器认证。

| 场景 | 结果 |
| --- | --- |
| 3D 场景加载及节点点击 | 星点、云雾、实体、关系线正常显示，选择节点打开对应详情；浏览器未出现错误或警告。 |
| 选中、展开、切换中心 | 选中周瑜保持赤壁为中心；展开从 10 个对象 / 20 条关联变为 12 / 24；撤销恢复到 10 / 20。 |
| 镜头操作 | 放大使节点远离投影中心，居中可重新框入当前图谱。 |
| 别名搜索与返回 | 搜索“孔明”得到诸葛亮；进入详情后返回，保留关键词和结果。 |
| 详情深链接 | URL 包含 `entity`；刷新后仍打开对应完整内容。 |
| 人物和事件行动互跳 | 赤壁事件内点击黄盖行动，打开黄盖并高亮同一行动；事件不再重复打开自身。 |
| 来源阅读返回 | 打开具体来源卡后关闭，返回原人物完整内容。来源入口可按卷次和传名区分。 |
| 时间无匹配 | 应用 280 年后显示无已收录关系提示；重置后恢复关系。 |
| 导览历史 | 导览切换步骤后使用浏览器返回，步骤编号、问题与探索中心一并恢复。 |
| 手机详情抽屉 | 点击当前选中对象打开详情；收起后再次点击同一对象能重新打开。 |
| 保存恢复 | 保存 3D 视图后切换列表并刷新；从“我的探索”恢复时成功初始化 3D，加载提示结束。 |
| 分享 | 点击分享可复制公开链接，并显示成功反馈；公开参数隐私范围另由自动测试校验。 |
| 星云全屏 | 点击“全屏”后画布扩展到窗口，缩放和居中控件保留；点击人物后浮出详情，退出按钮移到面板左侧；点击退出或按 Esc 返回常规布局，选中对象保留。手机模拟视口下画布与详情抽屉也可用。 |

## 已实现但尚未做专项故障注入的分支

WebGL 初始化失败或上下文丢失转为列表，提供低画质重试；存储和剪贴板失败有反馈或手动复制入口；删除视图和取消收藏提供撤销。以上分支已经过源码检查，但本次没有通过修改浏览器底层配置逐一模拟故障。

## 后续验收范围

尚未完成真实手机上的帧时间、首屏加载、长时间内存和能耗测量，也未完成 Safari / Firefox 专项兼容测试。不能据此宣称已达到技术方案全部性能指标。此阶段内容为 25 个实体、50 条关系、11 项来源和 3 个导览的本地待审校样本；内容后台、跨设备同步、正式发布流程与 Blender 资产仍属于后续范围。

## 《史记》楚汉专题接入

接入范围限定在公元前 209—202 年的楚汉之际：新增 10 位人物、6 个事件、28 条关系、7 项篇章来源和 2 个导览，以鸿门宴为默认中心。它与赤壁专题分开，来源入口指向《史记》对应本纪、世家、列传和月表的公开转录；关系区分史传记载与编辑解释。全部内容合计 41 个实体、78 条关系、18 项来源和 5 个导览，仍为待审校样本。

自动检查：`npm test` 有 3 个测试文件、27 项测试通过；`npm run build` 通过 TypeScript 检查与生产构建。新增测试覆盖跨专题引用隔离、公元前时间与分享链接往返、旧赤壁链接恢复。Three.js 分包仍有超过默认 500 kB 的构建体积提示，不影响构建通过。

在 Codex 内置浏览器完成以下桌面与手机模拟交互检查：

| 场景 | 实测结果 |
| --- | --- |
| 桌面 3D 星云（视口约 940 px） | 《史记》图谱显示 8 个节点、14 条关系；点击“项羽”后出现人物详情与正确的公元前年份。 |
| 原文入口 | 项羽详情中的来源卡指向《史记》卷七《项羽本纪》（`史記/卷007`）。 |
| 手机模拟视口（360 × 780） | 《史记》列表及公元前时间轴可见；面包屑打开专题选择器，切回“三国”后恢复公元 184—280 年时间范围。 |
| 跨专题搜索 | 在“三国”专题搜索繁体“項羽”，结果显示《史记》人物；进入详情后专题和 URL 同步切换。 |
| 页面宽度 | 只读 DOM 测量中，360 px 视口与文档宽度相同，未出现横向溢出。 |
| 导览与公元前筛选 | 《史记》导览可从鸿门首步进入项羽第二步；选中公元前 202 年并应用后，图谱收敛到项羽、垓下之战及当年关系。 |

以上是内置浏览器和手机模拟视口结果，尚未在实体手机或 Safari / Firefox 上验证。导览完整流程、复杂历史返回路径及真实性能仍需后续专项检查。来源核对范围和扩展方式见[《史记》接入技术说明](史记接入技术说明.md)。
